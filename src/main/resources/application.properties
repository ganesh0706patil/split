# Give your application a name
spring.application.name=split

# === DATABASE CONFIGURATION (HARDCODED FOR RAILWAY) ===
# Using the public connection URL directly.

# The JDBC URL format is jdbc:postgresql://HOST:PORT/DATABASE_NAME
spring.datasource.url=jdbc:postgresql://turntable.proxy.rlwy.net:27412/railway

# The username is 'postgres'
spring.datasource.username=postgres
# The password from your connection string
spring.datasource.password=QWyvCufDIDIBwUFFZyybPzyLcfXxdrZq

# --- Keep your other JPA/Hibernate settings ---
# 'update' is fine for development. It will create/update tables on startup.
spring.jpa.hibernate.ddl-auto=update
# It's good to see the SQL queries in the deployment logs for debugging
spring.jpa.show-sql=true
# Use a standard naming strategy
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
# Best practice to disable Open Session in View
spring.jpa.open-in-view=false